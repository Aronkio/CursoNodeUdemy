'use strict'

//bucle for
//loop es una estructura de contro que se repite varias veces

var numeros = 100;

for (let i = 0; i <= numeros; i++) {
    console.log("vamos por el numero " + i);

    // debugger; ver que esta pasando en el bucle
}